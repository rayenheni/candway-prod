// Candway Frontend — PostCSS configuration
// Tailwind CSS v4 uses the @tailwindcss/vite Vite plugin (not the PostCSS plugin).
// This file overrides the root postcss.config.js (which uses the old v3 plugin)
// so that Vite does not accidentally load the incompatible legacy config.
export default {};
